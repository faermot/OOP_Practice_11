<UserControl x:Class="OOP_Practice_11.View.UserControls.OrderUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d"
             Name="orderUC">

    <Border Padding="5" Margin="0 3" HorizontalAlignment="Stretch">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="Transparent"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#364C8F"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <StackPanel Orientation="Horizontal">
            <Grid Width="42" Height="42" Margin="12 0 0 0">
                <Border CornerRadius="10" Background="#152457"/>
                <fa:ImageAwesome Icon="{Binding Path=Icon, ElementName=orderUC}"
                                 Foreground="#BCC6E6"
                                 Width="20"/>
            </Grid>
            <StackPanel Margin="10 0 0 0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Path=Title, ElementName=orderUC}"
                           FontSize="13"
                           Foreground="#7C8DC3"/>
                <TextBlock Text="{Binding Path=Desc, ElementName=orderUC}"
                           FontSize="11"
                           Foreground="#7C8DC3"
                           Margin="0 3 0 0"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
